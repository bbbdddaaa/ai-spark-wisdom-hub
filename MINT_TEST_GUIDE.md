# Mint 功能测试指南

## 🔍 问题诊断结果

### 发现的问题

1. **API 地址配置错误** ✅ 已修复
   - 之前：`VITE_API_URL=https://api.aispark.space`（生产环境）
   - 现在：`VITE_API_URL=http://localhost:3100`（本地开发）

2. **链上资格未授予** ✅ 已添加测试按钮
   - 数据库显示有资格：`true`
   - 链上实际资格：`false` ← Mint 失败的根本原因
   - 合约会检查链上资格，不是数据库资格

3. **评分拦截** ✅ 已去除
   - 测试期间暂时注释掉评分不足的拦截

---

## 🧪 测试功能

### 新增：手动授予 Mint 资格按钮

在 **MintPanel** 中新增了一个蓝色测试按钮，当检测到链上没有资格时自动显示。

**功能：**
- 点击按钮自动调用后端 API
- 授予当前连接钱包的链上 mint 资格
- 授予成功后 2 秒自动刷新页面

**显示条件：**
- ✅ 钱包已连接
- ✅ 链上资格为 `false`（未授予）

---

## 📝 完整测试流程

### 步骤 1：启动服务

**终端 1 - 启动后端 API：**
```bash
npm run api
```

**终端 2 - 启动前端：**
```bash
npm run dev
```

或者使用一键启动：
```bash
npm start
```

---

### 步骤 2：连接钱包

1. 访问 `http://localhost:5173`
2. 点击"连接钱包"
3. 确保连接到 **Base 主网**（链 ID: 8453）

---

### 步骤 3：测试授予资格（两种方式）

#### 方式 A：通过发帖自动授予（推荐）

1. **发布第一篇帖子**
   - 点击"Create Post"
   - 输入标题和内容
   - 点击"Analyze with AI"
   - 点击"Publish"（评分拦截已去除）

2. **查看日志**
   - 打开浏览器控制台
   - 应该看到：
     ```
     🎉 用户 0x... 发布第一篇帖子，准备授予mint资格
     📡 调用 API: http://localhost:3100/api/grant-eligibility
     ✅ 成功授予mint资格
     ```

3. **查看后端日志**
   - 后端终端应显示：
     ```
     📝 收到授予资格请求: 0x...
     🔄 正在授予资格: 0x...
     ✅ 交易已发送: 0x...
     🎉 授予资格成功
     ```

#### 方式 B：使用测试按钮手动授予

1. **打开 Mint Panel**
   - 点击页面上的 "Mint" 按钮或标签

2. **查看状态**
   - 如果链上没有资格，会显示蓝色测试按钮：
     ```
     🧪 开发测试：链上资格未授予
     [手动授予 Mint 资格]
     ```

3. **点击测试按钮**
   - 按钮显示"授予中..."
   - 等待 API 调用完成

4. **查看结果**
   - 成功：显示绿色提示，页面 2 秒后自动刷新
   - 失败：显示红色错误信息

---

### 步骤 4：验证资格

刷新页面后，在 Mint Panel 中检查：

```
⛓️ 链上状态（合约）:
├─ 有资格(链): true   ✅
├─ 已Mint(链): false
├─ 可以Mint(链): true  ✅
```

---

### 步骤 5：测试 Mint

1. **确认状态**
   - ✅ 链上有资格
   - ✅ 未 Mint
   - ✅ ETH 余额 ≥ 0.003 ETH

2. **点击 Mint 按钮**
   ```
   [Mint 10,000 SPARK]
   ```

3. **确认交易**
   - 钱包弹出确认窗口
   - 检查交易详情：
     - 合约：`0x98F4a496ac7a5796cB6617401c9DBaFc50d5D839`
     - 金额：`0.003 ETH`
     - Gas：自动估算

4. **等待确认**
   - 交易提交后显示交易哈希
   - 等待区块链确认（约 2-5 秒）

5. **验证结果**
   - ✅ 获得 10,000 SPARK
   - ✅ Mint 状态变为 `true`
   - ✅ 按钮显示"Minted"

---

## 🔧 故障排查

### 问题 1：API 调用失败 `net::ERR_NAME_NOT_RESOLVED`

**原因：** `VITE_API_URL` 配置错误

**解决：**
```bash
# 检查配置
grep VITE_API_URL .env.local

# 应该显示
VITE_API_URL=http://localhost:3100

# 如果不对，修改
sed -i '' 's|VITE_API_URL=.*|VITE_API_URL=http://localhost:3100|' .env.local

# 重启前端
npm run dev
```

---

### 问题 2：Mint 时钱包显示"合约执行失败"

**原因：** 链上没有资格

**检查：**
打开控制台，查看日志：
```
⛓️ 链上状态（合约）:
├─ 有资格(链): false  ← 这是问题！
├─ 可以Mint(链): false
```

**解决：**
1. 使用测试按钮手动授予资格
2. 或者发布第一篇帖子自动授予

---

### 问题 3：后端 API 未运行

**症状：**
```
❌ 授予mint资格API调用失败
Failed to fetch
```

**解决：**
```bash
# 检查 API 是否运行
curl http://localhost:3100/health

# 如果失败，启动 API
npm run api
```

---

### 问题 4：数据库和链上状态不一致

**症状：**
```
⚠️ 警告：数据库资格状态与链上不一致！
   数据库: true
   链上: false
```

**说明：**
- 这是正常现象
- 发帖时会更新数据库，但链上授权可能延迟
- 使用测试按钮手动同步即可

---

## 📊 使用脚本检查状态

使用命令行工具检查用户状态：

```bash
# 检查指定地址的 mint 状态
npx tsx scripts/check-mint-status.ts 0x你的地址

# 示例输出
⛓️ 链上状态:
✅ 有mint资格: 是
✅ 已经mint: 否
✅ 可以mint: 是
📊 已mint人数: 1
📊 剩余名额: 1999
```

---

## 🎯 测试检查清单

测试前确保：

- [ ] 后端 API 运行在 `http://localhost:3100`
- [ ] 前端运行在 `http://localhost:5173`
- [ ] `.env.local` 中 `VITE_API_URL=http://localhost:3100`
- [ ] `.env.local` 中配置了 `OWNER_PRIVATE_KEY`
- [ ] 钱包连接到 Base 主网
- [ ] 钱包有足够的 ETH（≥ 0.004 ETH，包括 gas）

测试完成后检查：

- [ ] 发帖后自动调用了授权 API
- [ ] 链上资格显示 `true`
- [ ] 可以成功 Mint
- [ ] 获得了 10,000 SPARK
- [ ] Mint 状态更新为 `true`

---

## 🚀 恢复生产配置

测试完成后，记得：

1. **恢复评分拦截**（如果需要）
   - 在 `PostForm.tsx` 中取消注释评分检查

2. **移除测试按钮**（可选）
   - 在 `MintPanel.tsx` 中删除测试按钮相关代码

3. **更新 API 地址**（生产部署时）
   ```bash
   # 更新为生产 API 地址
   VITE_API_URL=https://api.aispark.space
   ```

---

## 📝 开发笔记

### 改动清单

1. ✅ 修复 `.env.local` 中的 `VITE_API_URL`
2. ✅ 在 `PostForm.tsx` 中注释掉评分拦截
3. ✅ 在 `MintPanel.tsx` 中添加详细日志
4. ✅ 在 `MintPanel.tsx` 中添加链上状态检查
5. ✅ 在 `MintPanel.tsx` 中添加测试按钮
6. ✅ 创建 `scripts/check-mint-status.ts` 脚本

### 日志说明

前端会输出详细的调试信息：
- 🎯 Mint 流程开始
- 📊 状态检查（数据库 + 链上）
- 💰 资金状态
- 📝 合约信息
- 🚀 交易配置
- ✅/❌ 执行结果

后端会输出：
- 📝 收到请求
- 🔄 正在授予资格
- ✅ 交易已发送
- 🎉 授予成功
- ⏱️ 处理时间
